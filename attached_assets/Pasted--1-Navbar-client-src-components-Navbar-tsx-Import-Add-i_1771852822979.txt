## 1. Navbar (`client/src/components/Navbar.tsx`)
- **Import**: Add `import logoImg from "@/assets/logo.png";` at the top.
- **Logo Container**: Find the `Link` containing the logo. Remove the `p-1 bg-white rounded-lg` classes from the `div` wrapping the `img` tag to allow the logo to blend with the background.
- **Logo Image**: Update the `img` tag to use `src={logoImg}` and set its size to `className="w-14 h-14 object-contain"`.
- **Branding Text**: Remove the `flex flex-col` wrapper around the name. Use a single `span` with `whitespace-nowrap` for "Career Goal Academy" to keep it on one line.

## 2. Footer (`client/src/components/Footer.tsx`)
- **Import**: Add `import logoImg from "@/assets/logo.png";` at the top.
- **Logo Container**: Remove the `p-1 bg-white rounded-lg` wrapper around the logo image.
- **Logo Image**: Update the `img` tag to use `src={logoImg}` and set its size to `className="w-12 h-12 object-contain"`.
- **Branding Text**: Ensure the text `Career Goal Academy` has the `whitespace-nowrap` class.

## 3. Login Page (`client/src/pages/AuthPage.tsx`)
- **Import**: Add `import logoImg from "@/assets/logo.png";` at the top.
- **Header Section**: 
  - Replace the `GraduationCap` icon with `<img src={logoImg} alt="CGA Logo" className="w-20 h-20 object-contain" />`.
  - Remove the `bg-primary/10 rounded-2xl` styling from the container to let the logo blend.
- **Branding Text**: Change "Welcome to CGA" to "Career Goal Academy" and add `whitespace-nowrap`.
- **Subtitle**: Delete the `<p>` tag containing "Academy Management System".

## 4. Student Dashboard (`client/src/pages/Dashboard.tsx`)
- **Import**: Add `import logoImg from "@/assets/logo.png";` at the top.
- **Sidebar Branding**: 
  - Replace the `GraduationCap` icon with `<img src={logoImg} alt="CGA Logo" className="w-10 h-10 object-contain" />`.
  - Remove the `bg-white rounded-lg` wrapper from the logo container.
  - Consolidate the two `span` tags into one: `<span className="font-display font-bold text-base leading-none text-foreground whitespace-nowrap">Career Goal Academy</span>`.
- **Mobile Header**: Replace the `GraduationCap` icon with the logo image (`w-8 h-8`) and add `whitespace-nowrap` to the name.

## 5. Payment Page (`client/src/pages/Payment.tsx`)
- **Import**: Add `import logoImg from "@/assets/logo.png";` at the top.
- **Decorative Logo**: Update the background logo in the `CardHeader` to use `src={logoImg}` (at `w-24 h-24`).